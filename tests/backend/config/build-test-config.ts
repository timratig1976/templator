// Temporary shim during migration from test-suite -> tests
// Re-export from the legacy location so imports to `tests/config/build-test-config` work now
export { getBuildTestConfig } from '../../test-suite/build-test-config';
