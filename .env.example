# Database Configuration
# Copy this to .env or add these variables to your existing .env file

DATABASE_URL='postgresql://neondb_owner:npg_estu7adjCAb8@ep-small-heart-a2o3hxfb-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'
# OpenAI API Configuration
OPENAI_API_KEY='key'
# Production Note: Change all passwords before deploying to production!
# Backend API Configuration
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:3009
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

REPORTS_DIR=./reports
# Uploads dir resolves relative to the backend path eg: backend/storage/uploads
UPLOADS_DIR=storage/uploads

# Feature Flags (safe defaults)
# Enable new pipeline/step runtime logging and storage (shadow-writes only)
PIPELINE_LOGGING_ENABLED=true
# IR validation mode: off | log | enforce
# off/log keep behavior unchanged for existing UI; enforce will fail steps on invalid IR
IR_VALIDATION_MODE=off
