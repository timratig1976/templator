# Test environment variables for backend
# Use a safe dummy key so integration tests that import OpenAI client can run without throwing
OPENAI_API_KEY=dummy-test-key

# Optional test toggles (recognized by code where applicable)
NODE_ENV=test
